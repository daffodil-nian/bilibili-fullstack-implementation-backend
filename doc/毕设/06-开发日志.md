# 06 开发日志

> 与 `readme.md`「开发计划」同步思路；此处可写稍细，供论文「系统实现过程」使用。  
> 只记做过什么、踩过什么坑，不必写成长日记。

---

## 2026-08-30

### 完成

- 登录 / 注册、修改用户信息：基本完成（用户模块）  
- 专栏发布：`publish` + 标签关联 + 已发布同步 ES  
- 专栏详情：`detail`（权限、作者、标签、hasLiked、Redis 点击量）  
- 专栏互动表设计与落地：`article_coin` / `article_favorite` / `article_share`；`articles` 增加 `coin_count`、`favorite_count`  
- 评论区 VO：`CommentRootVO`、`CommentReplyVO`  
- 文档：`doc/06-Redis操作笔记.md`；本毕设文档目录初始化  

### 问题与处理

- 详情查询报错 `Unknown column 'coin_count'`：Java 实体已加字段，旧库未 ALTER → 为 `articles` 增加 `coin_count`、`favorite_count` 后测试通过  

### 未完成 / 下一步

- `ArticleController` 挂 HTTP  
- `list`、点赞、投币、收藏、评论业务  
- 点击量定时刷回 MySQL  
- E-R 图绘制并放入 `doc/images/`  

---

## （模板）

## YYYY-MM-DD

### 完成
- 

### 问题与处理
- 

### 下一步
- 
