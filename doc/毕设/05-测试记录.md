# 05 测试记录

> 用途：积累毕设「系统测试」章节素材。每测一次加一行或一小节。  
> 更新日期：2026-08-30

---

## 1. 测试环境

| 项 | 说明 |
|---|---|
| 方式 | Spring Boot 测试（`@SpringBootTest`）等 |
| 配置 | `local` profile |
| MySQL | 库 `platform`（需表结构与实体一致） |
| Redis | 如 `192.168.58.128:6379` |
| JDK | 21 |

---

## 2. 用例记录

### 2026-08-30 专栏发布 + 详情

| 项 | 内容 |
|---|---|
| 用例名称 | 发布专栏后查询详情 |
| 前置条件 | MySQL、Redis 可用；`articles` 已包含 `coin_count`、`favorite_count` |
| 步骤 | 1. 调用发布 2. 用返回的 articleId 调 `detail` |
| 期望 | 发布返回 ID；详情返回正文、作者、标签、点击量等 |
| 实际结果 | **通过**（日志：发布专栏成功，专栏 ID 如 3；详情成功） |
| 备注 | 曾因库表缺少 `coin_count` 报 `Unknown column`；执行 ALTER 后通过 |

### （模板，复制用）

| 项 | 内容 |
|---|---|
| 日期 | |
| 用例名称 | |
| 前置条件 | |
| 步骤 | |
| 期望 | |
| 实际结果 | 通过 / 失败 |
| 截图 | 放到 `doc/images/test-YYYYMMDD-xxx.png` |
| 备注 | |

---

## 3. 已知问题（测试中发现）

| 日期 | 问题 | 处理 |
|---|---|---|
| 2026-08-30 | 实体有 `coin_count`，旧库无该列 | 对 `articles` 执行 ALTER 增加列 |
| — | `UserWallet` 无 `@TableId` 警告 | 记录；不影响本次详情测试 |
| — | `list` / 评论 / 投币等未测 | 功能未完成 |

---

## 4. 待测清单

- [ ] 私密专栏：非作者 detail 应失败  
- [ ] 点击量连续访问 Redis 递增  
- [ ] 点赞 toggle  
- [ ] 投币扣余额与重复投币拒绝  
- [ ] 评论一级 / 二级  
- [ ] 搜索专栏  

---

## 5. 论文写作提示

测试章节通常需要：测试目的、环境、用例表、部分截图、结论。本文件表格可直接改编进论文。  
