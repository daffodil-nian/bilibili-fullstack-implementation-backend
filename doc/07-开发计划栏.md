## 2026-08-30

### 目标完成

- 登录 / 注册、修改用户信息：基本完成（用户模块）  
- 专栏发布：`publish` + 标签关联 + 已发布同步 ES  
- 专栏详情：`detail`（权限、作者、标签、hasLiked、Redis 点击量）  
- 专栏互动表设计与落地：`article_coin` / `article_favorite` / `article_share`；`articles` 增加 `coin_count`、`favorite_count`  
- 评论区 VO：`CommentRootVO`、`CommentReplyVO`  
- 文档：`doc/06-Redis操作笔记.md`；本毕设文档目录初始化  

## 2026-08-31

完善发专栏功能，评论，点赞，搜索专栏文章，点进去查看详情，后进行测试，完成之后再完善括号里面的功能以及对接前端

（转发、收藏、投币、关注用户）

熟悉redis的一些操作


## 2026-09-02
今天在写另一个项目，今天就写关注用户功能模块吧


## 2026-09-03  完善关注模块（part 1）
碎碎念：
1.今天修改了关注BUG，关注后粉丝数和关注数的动态变化功能实现。√
2.实现用户点击关注功能 √
3.实现用户点击个人首页能够看到用户信息
4.如果用户不想让人看到自己的粉丝列表，可以选择设置，这样就看不到了，反之，就可以查看关注数和粉丝数（开发完成，没测试）
5.上述功能前端要同时跟进
6.创建隐私管理表，默认不公开，公开就插入值（开发完成）
7.前端按钮样式修改 √

## 2026-09-04 继续完善关注模块（后端）
1.查看用户关注列表和粉丝列表功能完善√
2.测试上面的接口
3.对接前后端
【查看用户关注列表和粉丝列表，关注，更新自己隐私情况】√
【查看别人的主页，查看自己的隐私情况】
前后端跑通
4.同步把数据更新到ES中
5.【用户隐私表、关注用户，查看用户粉丝数、查看用户关注数（包括隐私管理）】测试完成√
6.学习如何开发IM通讯网站（todo）
7.继续完善需求分析文档

## 2026-09-05 发帖模块+引入netty（part1）
1.完善前端逻辑，对接后端接口

2.引入netty，为之后的聊天通讯功能做铺垫

3.看网课（IM项目）

今天要看的文档如下

netty介绍：

https://www.cnblogs.com/mic112/p/15527384.html

https://waylau.com/netty-4-user-guide/

https://cloud.tencent.com/developer/article/1748761

websocket 文档：

https://developer.mozilla.org/zh-CN/docs/Web/API/WebSocket

https://developer.mozilla.org/zh-CN/docs/Web/API/WebSockets_API/Writing_a_WebSocket_server_in_Java

## 2026-09-06 

今天继续学习IM的通信的一个原理，加入到我的毕业设计中。

学习别人的前端写法（）

实现聊天通讯的后端功能（把枚举类啥的写完即可）√



```java
public void handlerAdded(ChannelHandlerContext ctx) {
        ChannelPipeline cp = ctx.pipeline();
        if (cp.get(WebSocketServerProtocolHandshakeHandler.class) == null) {
            
            // 握手处理器
            cp.addBefore(ctx.name(), WebSocketServerProtocolHandshakeHandler.class.getName(), new WebSocketServerProtocolHandshakeHandler(this.serverConfig));
        }

        if (this.serverConfig.decoderConfig().withUTF8Validator() && cp.get(Utf8FrameValidator.class) == null) {
            cp.addBefore(ctx.name(), Utf8FrameValidator.class.getName(), new Utf8FrameValidator(this.serverConfig.decoderConfig().closeOnProtocolViolation()));
        }

    }

 public void channelRead(final ChannelHandlerContext ctx, Object msg) throws Exception {
        HttpObject httpObject = (HttpObject)msg;
        if (httpObject instanceof HttpRequest) {
            final HttpRequest req = (HttpRequest)httpObject;
            this.isWebSocketPath = this.isWebSocketPath(req);
            if (!this.isWebSocketPath) {
                ctx.fireChannelRead(msg);
                return;
            }

            try {
                WebSocketServerHandshakerFactory wsFactory = new WebSocketServerHandshakerFactory(getWebSocketLocation(ctx.pipeline(), req, this.serverConfig.websocketPath()), this.serverConfig.subprotocols(), this.serverConfig.decoderConfig());
                final WebSocketServerHandshaker handshaker = wsFactory.newHandshaker(req);
                final ChannelPromise localHandshakePromise = this.handshakePromise;
                if (handshaker == null) {
                    WebSocketServerHandshakerFactory.sendUnsupportedVersionResponse(ctx.channel());
                } else {
                    WebSocketServerProtocolHandler.setHandshaker(ctx.channel(), handshaker);
                    ctx.pipeline().remove(this);
                    ChannelFuture handshakeFuture = handshaker.handshake(ctx.channel(), req);
                    handshakeFuture.addListener(new ChannelFutureListener() {
                        public void operationComplete(ChannelFuture future) {
                            if (!future.isSuccess()) {
                                localHandshakePromise.tryFailure(future.cause());
                                ctx.fireExceptionCaught(future.cause());
                            } else {
                                localHandshakePromise.trySuccess();
                                ctx.fireUserEventTriggered(ServerHandshakeStateEvent.HANDSHAKE_COMPLETE);
                                ctx.fireUserEventTriggered(new WebSocketServerProtocolHandler.HandshakeComplete(req.uri(), req.headers(), handshaker.selectedSubprotocol()));
                            }

                        }
                    });
                    this.applyHandshakeTimeout();
                }
            } finally {
                ReferenceCountUtil.release(req);
            }
        } else if (!this.isWebSocketPath) {
            ctx.fireChannelRead(msg);
        } else {
            ReferenceCountUtil.release(msg);
        }

    }
```





## 2026-09-07

今天开发计划如下：

1.先跑通两个用户之间的单聊通信（目前已经开发到两个人之间单聊）

2.再完善用户的收藏功能

3.有空补充前端





**关注一个人**

关注一个人就会收到他的自动回复，关注之后要先判断是否是互关的，如果不是互关就要调用MessageDao,RoomFriendDao,RoomDao和ContactDao

关注成功

  → RoomDao 建房（没有才建）

  → RoomFriendDao 绑两个人

  → ContactDao 双方各一条

  → MessageDao 插一条自动消息（比如「我关注了你」）

**互关**



现在还在需求分析阶段：

1，用户关注了一个人-》调用chatService的接口，首先判断两个人是否互相关注，如果互相关注就让先关注的那一方发送一条消息，如果没有互相关注首先要建房，然后绑定两个人之间的关系，接下来在双方的Contact中出现，最后，如果被关注的人如果有一个默认的语句就可以自动发送一条消息。



**疑惑：**

有一个疑问，如果createFriendSession是存在单聊房间的，那这个存在几种情况，第一种，只是单方面的关注，第二种，两个人互相关注了，再点击就是取关了

为了解决我的疑惑，我特意去BILIBILI网站上试验了，实验结果为：取关也不会删除和联系人之间的会话和房间





**采取的方案**

为了更好区分无记录和关注中的状态，我分成两个方法分别实现。

## **2026-09-08**

示例代码

**netty**

```java

/** uid -> 当前 WebSocket Channel（单端先够用；多端可改成 Set<Channel>） */
public class OnlineWsMap {
    private static final ConcurrentHashMap<Long, Channel> ONLINE = new ConcurrentHashMap<>();

    public static void put(Long uid, Channel channel) {
        ONLINE.put(uid, channel);
    }

    public static void remove(Long uid) {
        ONLINE.remove(uid);
    }

    public static void removeIfMatch(Long uid, Channel channel) {
        ONLINE.computeIfPresent(uid, (k, old) -> old == channel ? null : old);
    }

    public static Channel get(Long uid) {
        return ONLINE.get(uid);
    }
}
```

**握手抠token+IP**

```java
public class HttpHeadersHandler extends ChannelInboundHandlerAdapter {

    @Override
    public void channelRead(ChannelHandlerContext ctx, Object msg) throws Exception {
        if (msg instanceof FullHttpRequest request) {
            UrlBuilder urlBuilder = UrlBuilder.ofHttp(request.uri());

            String token = Optional.ofNullable(urlBuilder.getQuery())
                    .map(q -> q.get("token"))
                    .map(CharSequence::toString)
                    .orElse("");
            NettyUtil.setAttr(ctx.channel(), NettyUtil.TOKEN, token);

            // 去掉 query，只留 /ws 给协议升级
            request.setUri(urlBuilder.getPath().toString());

            String ip = request.headers().get("X-Real-IP");
            if (StrUtil.isBlank(ip)) {
                InetSocketAddress addr = (InetSocketAddress) ctx.channel().remoteAddress();
                ip = addr.getAddress().getHostAddress();
            }
            NettyUtil.setAttr(ctx.channel(), NettyUtil.IP, ip);

            ctx.pipeline().remove(this);
            ctx.fireChannelRead(request);
        } else {
            ctx.fireChannelRead(msg);
        }
    }
}
```

**JwtUtils**

```java
public Long getUidOrNull(String token) {
    if (token == null || token.isBlank()) {
        return null;
    }
    try {
        JWT jwt = JWT.of(token).setKey(secret.getBytes());
        if (!jwt.verify()) {
            return null;
        }
        Object uid = jwt.getPayload(UID_CLAIM);
        return uid == null ? null : Long.valueOf(uid.toString());
    } catch (Exception e) {
        return null;
    }
}
```

示例代码就写到这里



今日计划

1. 后端需求分析
2. 后端编码方案
3. 前后端对接（晚上对接）
4. 看lengyibai老师的wzry前端用到的技术栈和插件，学习ing ✔



今日阅读文章：

https://www.zhihu.com/question/514750005/answer/21021239469

**ASCII = American Standard Code for Information Interchange，美国信息交换标准代码**

https://zhuanlan.zhihu.com/p/554933865

**视频编码发展史**

https://www.bilibili.com/video/BV1FV4y1175P?t=56.9

https://github.com/dankogai/js-base64

https://www.topgoer.com/%E5%85%B6%E4%BB%96/%E5%8A%A0%E5%AF%86%E8%A7%A3%E5%AF%86/base64.html





## 2026-09-09

- 写博客笔记（概率论），如果有空就写有关历史的博客笔记



## 2026-09-10

- [x] 前端对接后端的分页查看粉丝数和关注数的接口
- [ ] 后端完善收藏功能
- [ ] 有空继续把单聊优化，引入websocket，进行单元测试





## 2026-09-12

- [ ] 引入线程池的概念，减少线程时间上的开销。
- [ ] 选型IP更新机制，【刚刚从同学那里交换信息价值，像小红书，抖音这些平台IP是根据你发评论的IP而变化的，而不是说你登录了就自动给你切换】

这里的线程池引入是方便之后的聊天功能开发而设定。详细内容可以见这篇文档https://www.yuque.com/snab/mallchat/oo87m9gndo4yx2sw#z55d4

如果池子里面的线程挂了，怎么办呢？是不是要把异常处理写的清楚一点而不是摸棱两可的？所以要写一个类

- [x] 学习mallchat项目
- [ ] 继续改前端BUG（fix: 修改前端BUG）

https://www.bilibili.com/video/BV1SFQRBaEzC?t=72.7

目标：单聊能通

  \- A、B 连上 WS

  \- A 发文本，B 在线能收到

  \- 消息落库；离线只入库

意思就是如果用户是离线状态，消息就写入数据库中，等上线了再写入到websocket中的通道中。

所以今天继续完善这个功能点

其实消息模块顶层设计有很多是待设计的：怎么实现不同的用户之间使用websocket协议聊天通讯？2.聊天通讯的时序性怎么能够得到保证？3.消息的唯一性【用id表示】4.可靠ACK？5.推拉结合？6.怎么实现消息已读未读功能？





1. 两人已有好友会话（关注时 createFriendSession）
2. IMServer Pipeline 顺序对 + Handler 注入（不要 new）
3. 握手认出 uid（先 ?uid= 也行）
4. HandshakeComplete → OnlineWsMap.put
5. CHAT_SEND → chatService.sendText
6. sendText：校验房间 → 入库 → fromUid 正确 → OnlineWsMap.push 对方
7. 断线 → OnlineWsMap.remove

上述是单聊真正需要的一些方法，我们可以发现，我们的项目中已经有这个功能模块了，第二点也有，好像基本上都有

IM 单聊引入消息队列，并非单纯因为推拉操作更简单，而是借助生产者消费者模型解耦收发，同时利用 MQ 原生的消息持久化等能力，规避 Redis Pub/Sub 无法保存离线消息的短板。



#### 固定博客段落结构（技术博文最通用，适合讲解选型类文章，比如你这个 IM 单聊）

每一小节都可以套用这个小框架：

1. 抛出问题 / 大家常见误区（很多人以为 IM 单聊必须使用消息队列？）
2. 给出结论（先亮观点）
3. 解释原理
4. 多方案横向对比
5. 优缺点总结 + 适用场景
6. 一句话小结

【可以这么理解，首先很多人以为IM单聊必须使用消息队列，然后说结论：“不一定”。接下来就要解释原理，并且多种方案对比，总结完优缺点最后一句话小结，6个步骤是技术博客的一个写法套路】

> 很多同学在实现 IM 单聊的时候，会认为单聊必须引入消息队列。 实际上单聊并不是一定要使用 MQ，Redis Pub/Sub 也可以实现基础的消息发布订阅，二者选型取决于业务规模与可靠性要求。
>
> 为什么不少 IM 系统会选择消息队列？ 消息队列是典型生产者消费者模型，能够实现消息发送方与接收方的解耦。同时 MQ 原生提供消息持久化、消息 ACK、消息堆积与重试能力。消息发送之后可以持久保存，用户离线，上线后可以重新消费消息。
>
> 而 Redis Pub/Sub 虽然同样是发布订阅模式，能够实现在线消息推送，但它的消息不会**持久化**。一旦客户端断开订阅，期间的消息会直接丢失。如果要支持离线消息，需要我们额外使用 Redis List/ZSet 来存储离线消息，并且自己实现消息重试、消息去重等逻辑，增加业务代码复杂度。
>
> 简单总结： 不是 MQ 的推拉操作本身更简单，而是 MQ 自带大量消息可靠性能力，可以减少自研成本；Redis PubSub 适合小型演示项目，高并发、要求消息不丢失的 IM 场景，一般会考虑 MQ。



今天其实还是在过渡，整理东西，晚上整理毕设的一些介绍，用word文档写即可

## 2026-09-13

- [x] 引入rabbitmq，配置登录的账号密码
- [x] 然后配置消息推送交换机和pushQueue和绑定消息队列和交换机的方法
- [x] 向外写聊天的接口
- [ ] 晚上写消息队列的学习文档。
- [ ] 要完善聊天列表对接前端的渲染，然后listMessage，还有标记已读和未读，等方法

## 2026-09-14

- [x] 首先解决昨天的问题【可以找找解决方案哈哈哈ing】
- [ ] 然后补充RabbitMq笔记 & Netty的笔记
- [x] 对接前端

今天任务没那么重哈哈哈加油！！！

前端需求我大概说一下：![image-20260914162055198](C:\Users\lily\AppData\Roaming\Typora\typora-user-images\image-20260914162055198.png)

首先要做一个这样子的页面，然后时间越近的越靠前。（可以根据activeTime来排序ing）

![image-20260914163119318](C:\Users\lily\AppData\Roaming\Typora\typora-user-images\image-20260914163119318.png)

然后这是和用户之间的谈话，目前我这里只开发了文字功能，表情包功能暂未开发.

## 2026-09-15

- [x] 前端页面美化
- [ ] 增加撤回功能
- [ ] 继续写需求分析文档，定期同步更新数据到

**准备写需求分析文档**



## 2026-09-16

- [x] 增加撤回功能
- [ ] 准备写表情包管理功能，做相关的需求分析
